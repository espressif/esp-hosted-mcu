# 核心层重构实施计划

> **状态同步（2026-05）**：这份文档最初按“实施前计划”编写，但当前 worktree 已不再处于纯计划阶段。`host/core/` 目录、`host/port/esp-idf/` 与 `host/port/linux/` 适配层、`tests/` 下的核心层测试，以及 `examples/host_framework_validation/`、`examples/host_framework_iperf_validation/` 等验证样例都已经落地。
>
> 因此，本文档现在更适合作为“实施记录 + 设计意图 + 补漏清单”来阅读，而不是把每一条命令都当成需要从头重复执行的步骤。像“从 main 创建功能分支”“创建目录骨架”这类引导性步骤，主要保留其历史上下文价值。

> **For agentic workers:** Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task.

**Goal:** 建立完全平台无关的 `host/core/` 目录，包括类型系统、vtable 合约、wrapper 宏、安全防护、配置接口、TLV 握手协议，以及迁移现有源文件并移除所有 ESP-IDF 依赖。

**Architecture:** 自底向上构建——先定义类型和合约（纯头文件），再写安全防护（编译期 + 运行时），然后定义配置和协议，最后搬迁现有 `.c` 文件并逐文件移除 `esp_*` 引用。每个阶段完成后用 gcc 语法编译验证。

**Tech Stack:** C99, GCC (for verification), protobuf-c

**前置依赖:** 无（纯头文件阶段不依赖任何其他模块）

**产出:** `host/core/` 目录完整文件树，通过 `gcc -fsyntax-only` 编译隔离检查

---

## 开始前——分支管理与回滚策略

```bash
# 1. 从 main 创建功能分支（所有更改都在此分支上进行）
git checkout main
git checkout -b feat/core-layer-refactor

# 2. 如果实施中出现问题，可随时丢弃分支回到干净状态：
#    git checkout main && git branch -D feat/core-layer-refactor

# 3. 创建搬迁快照目录（用于 diff 验证和上游追踪）
mkdir -p .migration
```

---

## 目录创建

首先在仓库根目录下创建核心层的完整目录骨架：

```bash
mkdir -p host/core/include/h_public
mkdir -p host/core/include/h_internal
mkdir -p host/core/src
mkdir -p host/port/include
```

---

### Task 1: 平台无关基础类型 (`h_types.h`)

**Files:**
- Create: `host/core/include/h_public/h_types.h`

- [ ] **Step 1: 创建基础类型定义头文件**

```c
/*
 * ESP-Hosted Host Core — Platform-Independent Base Types
 *
 * This header defines the error code system, common types, and Wi-Fi mode/enum
 * replacements for all ESP-IDF-specific types. It is the first header included
 * by every core layer source file and is available to applications.
 */

#ifndef H_TYPES_H
#define H_TYPES_H

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>

/* ── Error Codes ──
 * POSIX errno semantics: zero = success, negative = error.
 * Port layer translates platform-native codes (pdTRUE/-EAGAIN/...) into these.
 * Core layer MUST NOT check platform-native return values directly. */
typedef int h_err_t;

#define H_OK                0
#define H_FAIL             -1
#define H_ERR_INVALID_ARG  -2
#define H_ERR_NO_MEM       -3
#define H_ERR_TIMEOUT      -4
#define H_ERR_NOT_SUP      -5
#define H_ERR_INVALID_STATE -6
#define H_ERR_BUSY         -7

/* ── MAC Address ── */
typedef uint8_t h_mac_addr_t[6];

/* ── Opaque Handles ──
 * Core layer never dereferences these; port implementations cast to their
 * platform-specific types internally. */
typedef void* h_thread_t;
typedef void* h_mutex_t;
typedef void* h_queue_t;
typedef void* h_semaphore_t;
typedef void* h_timer_t;

/* ── Wi-Fi Modes ── */
typedef enum {
    H_WIFI_MODE_NULL = 0,
    H_WIFI_MODE_STA,
    H_WIFI_MODE_AP,
    H_WIFI_MODE_APSTA,
    H_WIFI_MODE_MAX
} h_wifi_mode_t;

/* ── Wi-Fi Interface ── */
typedef enum {
    H_WIFI_IF_STA = 0,
    H_WIFI_IF_AP,
    H_WIFI_IF_NAN,
    H_WIFI_IF_MAX
} h_wifi_interface_t;

/* ── Wi-Fi Bandwidth ── */
typedef enum {
    H_WIFI_BW_20MHZ = 0,
    H_WIFI_BW_40MHZ,
    H_WIFI_BW_HT20 = H_WIFI_BW_20MHZ,  /* alias */
    H_WIFI_BW_HT40 = H_WIFI_BW_40MHZ,
} h_wifi_bandwidth_t;

/* ── Wi-Fi Power Save ── */
typedef enum {
    H_WIFI_PS_NONE = 0,
    H_WIFI_PS_MIN_MODEM,
    H_WIFI_PS_MAX_MODEM,
} h_wifi_ps_type_t;

/* ── Event Base ── */
typedef enum {
    H_EVENT_WIFI = 0,
    H_EVENT_IP,
    H_EVENT_HOSTED,
    H_EVENT_MAX
} h_event_base_t;

/* ── Event Handler Signature ── */
typedef void (*h_event_handler_t)(void *event_data, size_t event_data_size,
                                  void *user_ctx);

#endif /* H_TYPES_H */
```

- [ ] **Step 2: 语法验证**

```bash
gcc -fsyntax-only -I host/core/include/h_public host/core/include/h_public/h_types.h
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add host/core/include/h_public/h_types.h
git commit -m "feat(core): add platform-independent base types (h_types.h)

Define h_err_t, opaque handles, Wi-Fi enums replacing ESP-IDF types.
POSIX errno semantics for error codes."
```

---

### Task 2: Wi-Fi 类型系统 (`h_wifi_types.h`)

**Files:**
- Create: `host/core/include/h_public/h_wifi_types.h`

- [ ] **Step 1: 创建 Phase 1 最小 Wi-Fi 类型集合**

参考文档 4 §1 的类型映射表（10 个核心类型），字段与 ESP-IDF `wifi_*` 类型一一对应，但由核心层拥有定义权：

```c
#ifndef H_WIFI_TYPES_H
#define H_WIFI_TYPES_H

#include "h_types.h"
#include <stdint.h>

/* ── Wi-Fi Initialization Config ──
 * Replaces wifi_init_config_t.
 * ESP-IDF port adapter maps this → WIFI_INIT_CONFIG_DEFAULT() */
typedef struct {
    void     *rx_ba_win;       /* STATIC_RX_BUFFER_NUM */
    void     *tx_ba_win;       /* STATIC_TX_BUFFER_NUM */
    uint32_t  feature_caps;    /* Feature capability flags */
    uint32_t  sta_mgmt_buf;    /* mgmt buf for STA */
    uint16_t  tx_buf_num;      /* dynamic TX buffer number */
    uint16_t  rx_buf_num;      /* dynamic RX buffer number */
    uint8_t   static_rx_buf_num;
    uint8_t   static_tx_buf_num;
    uint8_t   cache_tx_buf_num;
    uint8_t   csi_enable;
    uint8_t   ampdu_rx_enable;
    uint8_t   ampdu_tx_enable;
    uint8_t   nvs_enable;
    uint8_t   nano_enable;
    uint32_t  magic;           /* WIFI_INIT_CONFIG_MAGIC equivalent */
    uint8_t   rx_ba_win_num;
    uint8_t   tx_ba_win_num;
    uint8_t   rx_ba_win_num_11ax;
    uint8_t   tx_ba_win_num_11ax;
    int       wifi_task_core_id;
} h_wifi_init_config_t;

/* ── Wi-Fi Configuration ──
 * Replaces wifi_config_t (union of sta_config and ap_config).
 * Phase 1: fields cover STA and SoftAP modes. */
typedef struct {
    /* STA config */
    struct {
        uint8_t  ssid[32];
        uint8_t  ssid_len;
        uint8_t  password[64];
        uint8_t  bssid[6];
        uint8_t  channel;
        uint8_t  listen_interval;
        uint8_t  pmf_cfg_capable;
        uint8_t  pmf_cfg_required;
    } sta;

    /* AP config */
    struct {
        uint8_t  ssid[32];
        uint8_t  ssid_len;
        uint8_t  password[64];
        uint8_t  channel;
        uint8_t  hidden_ssid;
        uint8_t  max_connection;
        uint16_t beacon_interval;
    } ap;
} h_wifi_config_t;

/* ── Scan Config ──
 * Replaces wifi_scan_config_t. */
typedef struct {
    uint8_t  *bssid;          /* NULL = scan all */
    uint8_t  *ssid;           /* NULL = scan all */
    uint8_t  ssid_len;
    uint8_t  channel;         /* 0 = scan all channels */
    bool     show_hidden;
    uint16_t active_scan_min_time;
    uint16_t active_scan_max_time;
    uint16_t passive_scan_time;
    uint8_t  home_chan_dwell_time;
} h_wifi_scan_config_t;

/* ── AP Record (Scan Result) ──
 * Replaces wifi_ap_record_t. Phase 1: essential fields only. */
typedef struct {
    uint8_t  bssid[6];
    uint8_t  ssid[32];
    uint8_t  ssid_len;
    uint8_t  primary_channel;
    uint8_t  second_channel;
    int8_t   rssi;
    uint8_t  authmode;
    uint8_t  pairwise_cipher;
    uint8_t  group_cipher;
    uint16_t beacon_interval;
    uint8_t  country[3];
    uint8_t  country_len;
    uint8_t  phy_11b : 1;
    uint8_t  phy_11g : 1;
    uint8_t  phy_11n : 1;
    uint8_t  phy_lr  : 1;
    uint8_t  wps     : 1;
    uint8_t  reserved : 3;
} h_wifi_ap_record_t;

/* ── STA List ──
 * Replaces wifi_sta_list_t. */
typedef struct {
    struct {
        uint8_t  mac[6];
        int8_t   rssi;
    } sta[10];              /* Phase 1: max 10 stations */
    uint8_t num;
} h_wifi_sta_list_t;

/* ── Country Config ──
 * Replaces wifi_country_t. */
typedef struct {
    uint8_t cc[3];           /* ISO 3166-1 alpha-2 + '\0' */
    uint8_t schan;           /* start channel */
    uint8_t nchan;           /* total channels */
    uint8_t max_tx_power;    /* dBm */
    uint8_t policy;          /* country policy */
} h_wifi_country_t;

#endif /* H_WIFI_TYPES_H */
```

- [ ] **Step 2: 语法验证**

```bash
gcc -fsyntax-only -I host/core/include/h_public host/core/include/h_public/h_wifi_types.h
```

- [ ] **Step 3: Commit**

```bash
git add host/core/include/h_public/h_wifi_types.h
git commit -m "feat(core): add platform-independent Wi-Fi types (h_wifi_types.h)

Phase 1: 10 core types replacing ESP-IDF wifi_* equivalents.
Fields mirror ESP-IDF layout for zero-copy mapping on ESP-IDF port."
```

---

### Task 3: vtable 合约定义 (`h_port_contract.h`)

**Files:**
- Create: `host/port/include/h_port_contract.h`

- [ ] **Step 1: 创建三合约结构体定义**

```c
/*
 * ESP-Hosted Host Port — Contract Definitions
 *
 * Three vtables define the complete set of platform capabilities the core
 * layer needs. A port implementation creates three const global instances
 * of these structs. The core layer accesses them exclusively through the
 * wrapper macros in h_wrapper.h.
 *
 * Each vtable is independently replaceable — swap OSAL for testing without
 * touching transport, or mock event layer without touching OSAL.
 */

#ifndef H_PORT_CONTRACT_H
#define H_PORT_CONTRACT_H

#include "h_types.h"
#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

/* ── OSAL Contract ── */
typedef struct {
    /* Memory (required) */
    void* (*malloc)(size_t size);
    void* (*calloc)(size_t n, size_t size);
    void* (*realloc)(void *mem, size_t newsize);
    void  (*free)(void *ptr);
    void* (*memcpy)(void *dst, const void *src, size_t n);
    void* (*memset)(void *s, int c, size_t n);
    void* (*malloc_align)(size_t size, size_t align);
    void  (*free_align)(void *ptr);

    /* Threads (required) */
    int (*thread_create)(const char *name, uint32_t prio, uint32_t stack,
                         void (*fn)(void*), void *arg, h_thread_t *out);
    int (*thread_delete)(h_thread_t thread);

    /* Mutex (required) */
    int (*mutex_create)(h_mutex_t *out);
    int (*mutex_lock)(h_mutex_t m, int32_t timeout_ms);
    int (*mutex_unlock)(h_mutex_t m);
    int (*mutex_delete)(h_mutex_t m);

    /* Queue (required) */
    int (*queue_create)(uint32_t count, uint32_t item_size, h_queue_t *out);
    int (*queue_send)(h_queue_t q, const void *item, int32_t timeout_ms);
    int (*queue_recv)(h_queue_t q, void *item, int32_t timeout_ms);
    int (*queue_msg_waiting)(h_queue_t q);
    int (*queue_reset)(h_queue_t q);
    int (*queue_delete)(h_queue_t q);

    /* Semaphore (required) */
    int (*sem_create)(uint32_t max, uint32_t init, h_semaphore_t *out);
    int (*sem_take)(h_semaphore_t sem, int32_t timeout_ms);
    int (*sem_give)(h_semaphore_t sem);
    int (*sem_give_from_isr)(h_semaphore_t sem, void *isr_ctx);
    int (*sem_delete)(h_semaphore_t sem);

    /* Critical Section (required) */
    void (*enter_critical)(void);
    void (*exit_critical)(void);

    /* Timer (required) */
    int (*timer_create)(const char *name, h_timer_t *out);
    int (*timer_start)(h_timer_t t, uint32_t period_ms, bool periodic,
                       void (*cb)(void*), void *arg);
    int (*timer_stop)(h_timer_t t);
    int (*timer_delete)(h_timer_t t);
    uint64_t (*get_time_ms)(void);

    /* Time / Delay (required) */
    void (*msleep)(uint32_t ms);
    void (*usleep)(uint32_t us);
    void (*blocking_delay)(unsigned int iterations);

    /* Logging (required) */
    void (*log_write)(int level, const char *tag, const char *fmt, ...);
} h_osal_contract_t;

/* ── Event Contract ── */
typedef struct {
    int (*register_handler)(h_event_base_t base, int32_t event_id,
                            h_event_handler_t handler, void *user_ctx);
    int (*unregister_handler)(h_event_base_t base, int32_t event_id,
                              h_event_handler_t handler);
    int (*post)(h_event_base_t base, int32_t event_id,
                void *event_data, size_t event_data_size);
} h_event_contract_t;

/* ── Transport HAL Contract ──
 * One vtable covers all transport types. A port that only supports SPI
 * fills SPI fields and leaves SDIO/UART as NULL. The NULLs are guarded by
 * compile-time transport selection (#if H_TRANSPORT_IN_USE). */
typedef struct {
    /* Bus lifecycle */
    int (*init)(void **out_handle);
    int (*deinit)(void *handle);

    /* SPI */
    int (*spi_transfer)(void *handle, void *transfer_ctx);

    /* SDIO */
    int (*sdio_card_init)(void *handle, bool show_config);
    int (*sdio_read_reg)(void *handle, uint32_t reg, uint8_t *data,
                         uint16_t size, bool lock);
    int (*sdio_write_reg)(void *handle, uint32_t reg, uint8_t *data,
                          uint16_t size, bool lock);
    int (*sdio_read_block)(void *handle, uint32_t reg, uint8_t *data,
                           uint16_t size, bool lock);
    int (*sdio_write_block)(void *handle, uint32_t reg, uint8_t *data,
                            uint16_t size, bool lock);
    int (*sdio_wait_intr)(void *handle, uint32_t timeout_ms);

    /* UART */
    int (*uart_read)(void *handle, uint8_t *data, uint16_t size);
    int (*uart_write)(void *handle, uint8_t *data, uint16_t size);
    int (*uart_flush)(void *handle);

    /* GPIO */
    int (*gpio_config)(uint32_t pin, uint32_t mode);
    int (*gpio_set_intr)(uint32_t pin, uint32_t intr_type,
                         void (*isr)(void*), void *arg);
    int (*gpio_clear_intr)(uint32_t pin);
    int (*gpio_read)(uint32_t pin);
    int (*gpio_write)(uint32_t pin, uint32_t value);

    /* netif */
    int (*netif_create)(uint8_t if_type, uint8_t if_num);
    int (*netif_destroy)(uint8_t if_type, uint8_t if_num);
} h_transport_contract_t;

/* ── Global Contract Instances ──
 * Declared extern here; each port provides exactly one definition of each
 * in its own .c file. Core layer accesses via h_wrapper.h macros. */
extern const h_osal_contract_t      g_h_osal;
extern const h_event_contract_t     g_h_event;
extern const h_transport_contract_t g_h_transport;

#endif /* H_PORT_CONTRACT_H */
```

- [ ] **Step 2: 验证 h_port_contract.h 可独立编译**

```bash
gcc -fsyntax-only -I host/core/include/h_public -I host/port/include \
    host/port/include/h_port_contract.h
```

Expected: no errors (`h_types.h` is the only dependency).

- [ ] **Step 3: Commit**

```bash
git add host/port/include/h_port_contract.h
git commit -m "feat(port): add vtable contract definitions (h_port_contract.h)

Three vtables: h_osal_contract_t (OS primitives), h_event_contract_t (event
system), h_transport_contract_t (SPI/SDIO/UART/GPIO/netif). Unified transport
vtable with NULL for unused modes."
```

---

### Task 4: Wrapper 宏 (`h_wrapper.h`)

**Files:**
- Create: `host/port/include/h_wrapper.h`

- [ ] **Step 1: 创建宏封装层**

```c
/*
 * ESP-Hosted Host Port — Wrapper Macros
 *
 * Core layer code writes h_malloc(sz), not g_h_osal.malloc(sz).
 * The macros provide:
 *   - Shorter, readable names
 *   - Single indirection (2 CPU cycles on Cortex-M4, negligible)
 *   - Replaceable globals for testing (swap g_h_osal per test suite)
 */

#ifndef H_WRAPPER_H
#define H_WRAPPER_H

#include "h_port_contract.h"

/* ── OSAL ── */
#define h_malloc(sz)                    (g_h_osal.malloc(sz))
#define h_calloc(n, sz)                 (g_h_osal.calloc(n, sz))
#define h_realloc(m, ns)                (g_h_osal.realloc(m, ns))
#define h_free(p)                       (g_h_osal.free(p))
#define h_memcpy(d, s, n)               (g_h_osal.memcpy(d, s, n))
#define h_memset(p, c, n)               (g_h_osal.memset(p, c, n))
#define h_malloc_align(sz, al)          (g_h_osal.malloc_align(sz, al))
#define h_free_align(p)                 (g_h_osal.free_align(p))

#define h_thread_create(n,pr,st,fn,a,o) (g_h_osal.thread_create(n,pr,st,fn,a,o))
#define h_thread_delete(t)              (g_h_osal.thread_delete(t))

#define h_mutex_create(o)               (g_h_osal.mutex_create(o))
#define h_mutex_lock(m, to)             (g_h_osal.mutex_lock(m, to))
#define h_mutex_unlock(m)               (g_h_osal.mutex_unlock(m))
#define h_mutex_delete(m)               (g_h_osal.mutex_delete(m))

#define h_queue_create(c, s, o)         (g_h_osal.queue_create(c, s, o))
#define h_queue_send(q, i, to)          (g_h_osal.queue_send(q, i, to))
#define h_queue_recv(q, i, to)          (g_h_osal.queue_recv(q, i, to))
#define h_queue_msg_waiting(q)          (g_h_osal.queue_msg_waiting(q))
#define h_queue_reset(q)                (g_h_osal.queue_reset(q))
#define h_queue_delete(q)               (g_h_osal.queue_delete(q))

#define h_sem_create(mx, in, o)         (g_h_osal.sem_create(mx, in, o))
#define h_sem_take(s, to)               (g_h_osal.sem_take(s, to))
#define h_sem_give(s)                   (g_h_osal.sem_give(s))
#define h_sem_give_from_isr(s, ctx)     (g_h_osal.sem_give_from_isr(s, ctx))
#define h_sem_delete(s)                 (g_h_osal.sem_delete(s))

#define h_enter_critical()              (g_h_osal.enter_critical())
#define h_exit_critical()               (g_h_osal.exit_critical())

#define h_timer_create(n, o)            (g_h_osal.timer_create(n, o))
#define h_timer_start(t, ms, p, cb, a)  (g_h_osal.timer_start(t, ms, p, cb, a))
#define h_timer_stop(t)                 (g_h_osal.timer_stop(t))
#define h_timer_delete(t)               (g_h_osal.timer_delete(t))
#define h_get_time_ms()                 (g_h_osal.get_time_ms())

#define h_msleep(ms)                    (g_h_osal.msleep(ms))
#define h_usleep(us)                    (g_h_osal.usleep(us))
#define h_blocking_delay(n)             (g_h_osal.blocking_delay(n))

/* ── Log ── */
typedef enum {
    H_LOG_NONE = 0,
    H_LOG_ERROR,
    H_LOG_WARN,
    H_LOG_INFO,
    H_LOG_DEBUG,
    H_LOG_VERBOSE
} h_log_level_t;

#define H_LOGE(tag, fmt, ...)  g_h_osal.log_write(H_LOG_ERROR, tag, fmt, ##__VA_ARGS__)
#define H_LOGW(tag, fmt, ...)  g_h_osal.log_write(H_LOG_WARN,  tag, fmt, ##__VA_ARGS__)
#define H_LOGI(tag, fmt, ...)  g_h_osal.log_write(H_LOG_INFO,  tag, fmt, ##__VA_ARGS__)
#define H_LOGD(tag, fmt, ...)  g_h_osal.log_write(H_LOG_DEBUG, tag, fmt, ##__VA_ARGS__)
#define H_LOGV(tag, fmt, ...)  g_h_osal.log_write(H_LOG_VERBOSE, tag, fmt, ##__VA_ARGS__)

/* ── Event ── */
#define h_event_register(b, i, cb, ctx)  (g_h_event.register_handler(b, i, cb, ctx))
#define h_event_unregister(b, i, cb)     (g_h_event.unregister_handler(b, i, cb))
#define h_event_post(b, i, d, l)         (g_h_event.post(b, i, d, l))

/* ── Transport ── */
#define h_transport_init(o)              (g_h_transport.init(o))
#define h_transport_deinit(h)            (g_h_transport.deinit(h))

#define h_spi_transfer(h, ctx)           (g_h_transport.spi_transfer(h, ctx))

#define h_sdio_read_block(h,r,d,s,l)     (g_h_transport.sdio_read_block(h,r,d,s,l))
#define h_sdio_write_block(h,r,d,s,l)    (g_h_transport.sdio_write_block(h,r,d,s,l))
#define h_sdio_wait_intr(h, to)          (g_h_transport.sdio_wait_intr(h, to))

#define h_uart_read(h, d, s)             (g_h_transport.uart_read(h, d, s))
#define h_uart_write(h, d, s)            (g_h_transport.uart_write(h, d, s))

#define h_gpio_config(pin, mode)         (g_h_transport.gpio_config(pin, mode))
#define h_gpio_set_intr(pin, t, isr, a)  (g_h_transport.gpio_set_intr(pin, t, isr, a))
#define h_gpio_clear_intr(pin)           (g_h_transport.gpio_clear_intr(pin))
#define h_gpio_read(pin)                 (g_h_transport.gpio_read(pin))
#define h_gpio_write(pin, val)           (g_h_transport.gpio_write(pin, val))

#define h_netif_create(t, n)             (g_h_transport.netif_create(t, n))
#define h_netif_destroy(t, n)            (g_h_transport.netif_destroy(t, n))

/* ── Safe Callback (for optional vtable functions) ── */
#define H_VTABLE_CALL(ops, func, ...) \
    ((ops)->func ? (ops)->func(__VA_ARGS__) : H_ERR_NOT_SUP)

#endif /* H_WRAPPER_H */
```

- [ ] **Step 2: 验证 wrapper 和 contract 一起可编译**

```bash
gcc -fsyntax-only -I host/core/include/h_public -I host/port/include \
    host/port/include/h_wrapper.h
```

Expected: no errors (pulls in `h_port_contract.h` → `h_types.h`).

- [ ] **Step 3: Commit**

```bash
git add host/port/include/h_wrapper.h
git commit -m "feat(port): add wrapper macro layer (h_wrapper.h)

Core code writes h_malloc() not g_h_osal.malloc(). ~2 CPU cycles overhead.
Log macros (H_LOGE/H_LOGW/H_LOGI/H_LOGD/H_LOGV) replace ESP_LOG*."
```

---

### Task 5: 安全防护——合约校验 + init/deinit

**Files:**
- Create: `host/core/src/h_init.c`
- Create: `host/core/include/h_internal/h_init.h`

- [ ] **Step 1: 创建初始化头文件**

```c
/* host/core/include/h_internal/h_init.h */
#ifndef H_INIT_H
#define H_INIT_H

#include "h_types.h"

h_err_t h_hosted_init(void);
h_err_t h_hosted_deinit(void);

#endif
```

- [ ] **Step 2: 创建 init/deinit 实现**

```c
/* host/core/src/h_init.c */
#include "h_init.h"
#include "h_wrapper.h"

/* OSAL init/deinit stubs — filled during port implementation (Plan 2) */
extern h_err_t h_osal_init(void);
extern void    h_osal_deinit(void);
extern h_err_t h_event_init(void);
extern void    h_event_deinit(void);
extern h_err_t h_transport_init(void);
extern void    h_transport_deinit(void);
extern h_err_t h_rpc_core_init(void);
extern void    h_rpc_core_deinit(void);

static bool g_hosted_initialized = false;

/* ── Contract Validation (fail-fast at startup) ── */
static h_err_t h_validate_contracts(void)
{
    /* OSAL required functions */
    if (!g_h_osal.malloc || !g_h_osal.free || !g_h_osal.mutex_create ||
        !g_h_osal.queue_create || !g_h_osal.sem_create ||
        !g_h_osal.thread_create) {
        H_LOGE("INIT", "OSAL contract missing required functions");
        return H_ERR_INVAL;
    }

    /* Event required functions */
    if (!g_h_event.register_handler || !g_h_event.post) {
        H_LOGE("INIT", "Event contract missing required functions");
        return H_ERR_INVAL;
    }

    /* Transport: validate the specific transport selected at compile time */
#if H_TRANSPORT_IN_USE == H_TRANSPORT_SPI
    if (!g_h_transport.init || !g_h_transport.spi_transfer) {
        H_LOGE("INIT", "SPI transport contract missing required functions");
        return H_ERR_INVAL;
    }
#elif H_TRANSPORT_IN_USE == H_TRANSPORT_SDIO
    if (!g_h_transport.init || !g_h_transport.sdio_read_block) {
        H_LOGE("INIT", "SDIO transport contract missing required functions");
        return H_ERR_INVAL;
    }
#elif H_TRANSPORT_IN_USE == H_TRANSPORT_UART
    if (!g_h_transport.init || !g_h_transport.uart_read) {
        H_LOGE("INIT", "UART transport contract missing required functions");
        return H_ERR_INVAL;
    }
#endif

    H_LOGI("INIT", "All required vtable slots filled");
    return H_OK;
}

/* ── h_hosted_init ──
 * Explicit init entry point. Application calls this before any Wi-Fi API.
 * Uses goto-cleanup for partial-init rollback. */
h_err_t h_hosted_init(void)
{
    h_err_t err;

    err = h_validate_contracts();
    if (err != H_OK) return err;

    err = h_osal_init();
    if (err != H_OK) goto cleanup_osal;

    err = h_event_init();
    if (err != H_OK) goto cleanup_event;

    err = h_transport_init();
    if (err != H_OK) goto cleanup_transport;

    err = h_rpc_core_init();
    if (err != H_OK) goto cleanup_rpc;

    g_hosted_initialized = true;
    H_LOGI("INIT", "ESP-Hosted initialized");
    return H_OK;

cleanup_rpc:
    h_transport_deinit();
cleanup_transport:
    h_event_deinit();
cleanup_event:
    h_osal_deinit();
cleanup_osal:
    return err;
}

/* ── h_hosted_deinit ──
 * Reverse-order teardown. Idempotent (safe to call multiple times). */
h_err_t h_hosted_deinit(void)
{
    if (!g_hosted_initialized) return H_OK;

    h_rpc_core_deinit();
    h_transport_deinit();
    h_event_deinit();
    h_osal_deinit();

    g_hosted_initialized = false;
    return H_OK;
}
```

- [ ] **Step 3: 验证编译（此时 OSAL stub extern 未定义，仅语法检查）**

```bash
gcc -fsyntax-only -I host/core/include/h_public -I host/core/include/h_internal \
    -I host/port/include host/core/src/h_init.c
```

- [ ] **Step 4: Commit**

```bash
git add host/core/src/h_init.c host/core/include/h_internal/h_init.h
git commit -m "feat(core): add init/deinit with contract validation

h_hosted_init() validates vtables at startup (fail-fast), uses goto-cleanup
for rollback on partial failure. h_hosted_deinit() is idempotent."
```

---

### Task 6: 配置接口 (`h_config.h`)

**Files:**
- Create: `host/port/include/h_config.h`
- Create: `host/port/include/h_port_config.h` (template)

- [ ] **Step 1: 创建配置头文件**

```c
/* host/port/include/h_config.h */
#ifndef H_CONFIG_H
#define H_CONFIG_H

/* ── Transport Selection ── */
#define H_TRANSPORT_SPI     1
#define H_TRANSPORT_SDIO    2
#define H_TRANSPORT_SPI_HD  3
#define H_TRANSPORT_UART    4

#ifndef H_TRANSPORT_IN_USE
#error "H_TRANSPORT_IN_USE must be defined by port layer (h_port_config.h)"
#endif

/* ── Default Thread Config ──
 * Overridden by each port's h_port_config.h if needed. */
#ifndef H_DEFAULT_TASK_STACK
#define H_DEFAULT_TASK_STACK  4096
#endif
#ifndef H_DEFAULT_TASK_PRIO
#define H_DEFAULT_TASK_PRIO   5
#endif

/* ── Feature Flags (Phase 1) ──
 * Disabled by default; enabled per-feature in future phases. */
#ifndef H_FEATURE_DPP
#define H_FEATURE_DPP 0
#endif
#ifndef H_FEATURE_ENTERPRISE
#define H_FEATURE_ENTERPRISE 0
#endif

/* ── Port-Specific Overrides ── */
#include "h_port_config.h"

#endif /* H_CONFIG_H */
```

```c
/* host/port/include/h_port_config.h (TEMPLATE)
 * Each port copy/pastes this and fills in platform-specific values.
 * This template file is NOT compiled — it serves as documentation. */
#ifndef H_PORT_CONFIG_H
#define H_PORT_CONFIG_H

/* MUST define transport in use */
#define H_TRANSPORT_IN_USE  H_TRANSPORT_SPI

/* Platform identity (for diagnostic logging) */
#define H_PORT_NAME         "unknown"
#define H_PORT_VERSION      "0.0.0"
#define H_PORT_RTOS         "none"
#define H_PORT_RTOS_VER     "0.0.0"
#define H_PORT_CHIP         "unknown"
#define H_PORT_BUILD_DATE   __DATE__

/* Port-specific thread tuning (override H_DEFAULT_*) */
/* #define H_DEFAULT_TASK_STACK  8192 */
/* #define H_DEFAULT_TASK_PRIO   10 */

/* Phase 2 feature flags — explicitly 0 for Phase 1 */
#define H_FEATURE_BLUETOOTH  0
#define H_FEATURE_OTA        0
#define H_FEATURE_NETSPLIT   0

#endif /* H_PORT_CONFIG_H */
```

- [ ] **Step 2: 验证编译**

```bash
gcc -fsyntax-only -I host/core/include/h_public -I host/port/include \
    host/port/include/h_config.h 2>&1
# Expected: #error triggered because no port_config.h defines H_TRANSPORT_IN_USE
# This is correct behavior — h_port_config.h is provided by the actual port.
```

- [ ] **Step 3: Commit**

```bash
git add host/port/include/h_config.h host/port/include/h_port_config.h
git commit -m "feat(port): add config interface (h_config.h + h_port_config.h template)

Transport selection, thread defaults, feature flags. Each port provides its
own h_port_config.h (template included as documentation)."
```

---

### Task 7: 事件类型定义 + 内部事件发布接口

**Files:**
- Create: `host/core/include/h_public/h_event.h`
- Create: `host/core/include/h_internal/h_event_post.h`

- [ ] **Step 1: 创建公共事件头文件**

```c
/* host/core/include/h_public/h_event.h */
#ifndef H_EVENT_H
#define H_EVENT_H

#include "h_types.h"
#include "h_wifi_types.h"

/* ── Wi-Fi Event IDs ──
 * Sub-IDs for H_EVENT_WIFI base. Replaces WIFI_EVENT_* in ESP-IDF. */
#define H_EVENT_WIFI_READY              0
#define H_EVENT_WIFI_SCAN_DONE          1
#define H_EVENT_WIFI_STA_START          2
#define H_EVENT_WIFI_STA_STOP           3
#define H_EVENT_WIFI_STA_CONNECTED      4
#define H_EVENT_WIFI_STA_DISCONNECTED   5
#define H_EVENT_WIFI_AP_START           6
#define H_EVENT_WIFI_AP_STOP            7
#define H_EVENT_WIFI_AP_STACONNECTED    8
#define H_EVENT_WIFI_AP_STADISCONNECTED 9
#define H_EVENT_WIFI_STA_BEACON_TIMEOUT 10
#define H_EVENT_WIFI_STA_AUTH_TIMEOUT   11

/* ── IP Event IDs ── */
#define H_EVENT_IP_STA_GOT_IP           0
#define H_EVENT_IP_STA_LOST_IP          1
#define H_EVENT_IP_AP_STA_IP_ASSIGNED   2

/* ── Event Registration API (application-facing) ── */
#include "h_wrapper.h"  /* h_event_register / h_event_unregister */

#endif /* H_EVENT_H */
```

- [ ] **Step 2: 创建内部事件发布头文件**

```c
/* host/core/include/h_internal/h_event_post.h */
#ifndef H_EVENT_POST_H
#define H_EVENT_POST_H

#include "h_types.h"

/* Internal (core-layer only) event posting.
 * Applications use h_event_post via h_wrapper.h.
 * Core code calls this to route events from RPC layer to the port event bus. */
h_err_t h_event_post_internal(h_event_base_t base, int32_t event_id,
                              void *event_data, size_t event_data_size);

#endif
```

- [ ] **Step 3: Commit**

```bash
git add host/core/include/h_public/h_event.h \
        host/core/include/h_internal/h_event_post.h
git commit -m "feat(core): add event type definitions and internal post interface

Wi-Fi/IP event IDs replace ESP-IDF WIFI_EVENT_*/IP_EVENT_*."
```

---

### Task 8: TLV 握手协议 (`h_priv_event.h`)

**Files:**
- Create: `host/core/include/h_internal/h_priv_event.h`

- [ ] **Step 1: 创建私有事件协议头文件**

直接复制文档 4 §3 的完整定义：

```c
/* host/core/include/h_internal/h_priv_event.h
 *
 * Platform-independent TLV handshake protocol definitions.
 * Replaces esp_private/wifi.h in the existing codebase.
 *
 * After slave powers on, host and slave exchange TLV frames to negotiate
 * capabilities, chip ID, firmware version, queue sizes, and transport mode.
 * This protocol is specific to ESP-Hosted's custom transport layer. */

#ifndef H_PRIV_EVENT_H
#define H_PRIV_EVENT_H

#include <stdint.h>

/* ── Private Event Frame ── */
typedef struct {
    uint8_t  event_type;
    uint8_t *tlv_data;
    uint16_t tlv_len;
} h_priv_event_t;

/* ── Event Types ── */
#define H_PRIV_EVENT_INIT              0x01
#define H_PRIV_CAPABILITY              0x10
#define H_PRIV_CAP_EXT                 0x11
#define H_PRIV_FIRMWARE_CHIP_ID        0x20
#define H_PRIV_FIRMWARE_CHIP_ESP32     0x00
#define H_PRIV_FIRMWARE_CHIP_ESP32S2   0x01
#define H_PRIV_FIRMWARE_CHIP_ESP32C2   0x02
#define H_PRIV_FIRMWARE_CHIP_ESP32C3   0x03
#define H_PRIV_FIRMWARE_CHIP_ESP32C6   0x04
#define H_PRIV_FIRMWARE_CHIP_ESP32C5   0x05
#define H_PRIV_FIRMWARE_CHIP_ESP32S3   0x06
#define H_PRIV_FIRMWARE_CHIP_ESP32H2   0x07
#define H_PRIV_FIRMWARE_CHIP_ESP32H4   0x08
#define H_PRIV_FIRMWARE_CHIP_ESP32C61  0x09
#define H_PRIV_FIRMWARE_CHIP_ESP32P4   0x0A
#define H_PRIV_TEST_RAW_TP             0x30
#define H_PRIV_RX_Q_SIZE               0x40
#define H_PRIV_TX_Q_SIZE               0x41
#define H_PRIV_FIRMWARE_VERSION        0x50
#define H_PRIV_TRANS_SDIO_MODE         0x60

#endif /* H_PRIV_EVENT_H */
```

- [ ] **Step 2: Commit**

```bash
git add host/core/include/h_internal/h_priv_event.h
git commit -m "feat(core): add TLV handshake protocol definitions (h_priv_event.h)

Replaces esp_private/wifi.h. Platform-independent chip ID constants and
private event frame structure."
```

---

### Task 9: 核心层源文件搬迁——传输驱动

**Files:**
- Create: `host/core/src/h_transport_drv.c` (from `host/drivers/transport/transport_drv.c`, ~800 lines)
- Create: `host/core/src/h_transport_util.c` (from `host/drivers/transport/transport_util.c`, ~200 lines)
- Create: `host/core/include/h_internal/h_transport.h`

- [ ] **Step 0: 搬迁前快照 + 上游追踪**

```bash
# 创建搬迁前快照，用于事后 diff 验证替换完整性
mkdir -p .migration/transport_backup
cp host/drivers/transport/transport_drv.c    .migration/transport_backup/
cp host/drivers/transport/transport_drv.h    .migration/transport_backup/
cp host/drivers/transport/transport_util.c   .migration/transport_backup/
cp host/drivers/transport/transport_util.h   .migration/transport_backup/

# 记录上游 commit，便于后续 cherry-pick 上游修复
echo "# transport_drv upstream ref" > .migration/transport_upstream_ref.txt
git log --oneline -5 -- host/drivers/transport/ >> .migration/transport_upstream_ref.txt

# 在搬迁前先确认关键引用位置（这些是必须替换的地方）
echo "=== Current ESP-IDF references in transport_drv.c ===" > .migration/transport_refs_before.txt
grep -n 'esp_private/wifi\.h\|esp_wifi\.h\|esp_err_t\|ESP_LOG\|ESP_OK\|ESP_FAIL\|vTaskDelay\|heap_caps_malloc\|spi_device_transmit\|sdio_io_rw_extended\|uart_read_bytes' \
    host/drivers/transport/transport_drv.c >> .migration/transport_refs_before.txt

echo "=== Current ESP-IDF references in transport_util.c ===" >> .migration/transport_refs_before.txt
grep -n 'esp_private/wifi\.h\|esp_wifi\.h\|esp_err_t\|ESP_LOG\|ESP_OK\|ESP_FAIL\|vTaskDelay\|heap_caps_malloc' \
    host/drivers/transport/transport_util.c >> .migration/transport_refs_before.txt
```

- [ ] **Step 1: 创建 transport 内部头文件**

```c
/* host/core/include/h_internal/h_transport.h */
#ifndef H_TRANSPORT_H
#define H_TRANSPORT_H

#include "h_types.h"
#include <stdint.h>

/* Frame types from common/transport/esp_hosted_header.h */
#define ESP_STA_IF     1
#define ESP_AP_IF      2
#define ESP_SERIAL_IF  3
#define ESP_HCI_IF     4
#define ESP_PRIV_IF    5
#define ESP_TEST_IF    6

h_err_t h_transport_drv_process_frame(uint8_t *frame, uint16_t len);
h_err_t h_transport_drv_send_frame(uint8_t if_type, uint8_t *data, uint16_t len);

#endif
```

- [ ] **Step 2: 搬迁 `transport_drv.c`——逐项替换 ESP-IDF 依赖**

从 `host/drivers/transport/transport_drv.c` 复制内容到 `host/core/src/h_transport_drv.c`，然后替换所有 ESP-IDF 依赖。

**已知引用位置与替换方案**（这些是从代码分析中提取的实际引用）：

| 行号(约) | 原始引用 | 替换为 | 说明 |
|:---:|---------|--------|------|
| #include | `#include "esp_private/wifi.h"` | `#include "h_priv_event.h"` | TLV 握手协议去 ESP 化 |
| #include | `#include "esp_wifi.h"` | 删除 | Wi-Fi 类型已在 `h_wifi_types.h` 中定义 |
| #include | `#include "esp_task.h"` | 删除 | task 配置来自 `h_config.h` |
| #include | `#include "freertos/FreeRTOS.h"` | 删除 | 通过 OSAL vtable 访问 |
| ~50+ | `esp_priv_event_t` | `h_priv_event_t` | 类型替换 |
| ~50+ | `ESP_PRIV_EVENT_INIT` 等 15+ 宏 | `H_PRIV_EVENT_INIT` 等 | TLV 事件类型常量 |
| 多行 | `ESP_LOGI(tag, ...)` | `H_LOGI(tag, ...)` | 日志宏 |
| 多行 | `ESP_LOGE(tag, ...)` | `H_LOGE(tag, ...)` | 日志宏 |
| 多行 | `esp_err_t` | `h_err_t` | 返回类型 |
| 多行 | `ESP_OK` | `H_OK` | 返回值 |
| 多行 | `ESP_FAIL` | `H_FAIL` | 返回值 |
| ~6处 | `vTaskDelay(...)` | `h_msleep(...)` | FreeRTOS 延迟 → OSAL |
| SPI 路径 | `spi_device_transmit(...)` | `h_spi_transfer(...)` | SPI 传输 → wrapper 宏 |
| SDIO 路径 | `sdio_io_rw_extended(...)` | `h_sdio_read_block(...)` / `h_sdio_write_block(...)` | SDIO 传输 → wrapper 宏 |
| UART 路径 | `uart_read_bytes(...)` | `h_uart_read(...)` | UART 传输 → wrapper 宏 |

```bash
cp host/drivers/transport/transport_drv.c host/core/src/h_transport_drv.c

# 逐项替换后验证：
# 1. 无 esp_ 头文件引用
grep -n '#include.*esp_' host/core/src/h_transport_drv.c || echo "  OK: zero esp_ includes"
# 2. 无 ESP_LOG 宏
grep -n 'ESP_LOG' host/core/src/h_transport_drv.c || echo "  OK: zero ESP_LOG"
# 3. 无 esp_err_t
grep -n 'esp_err_t' host/core/src/h_transport_drv.c || echo "  OK: zero esp_err_t"
# 4. 无 vTaskDelay
grep -n 'vTaskDelay\|FreeRTOS' host/core/src/h_transport_drv.c || echo "  OK: zero FreeRTOS"
# 5. 无 heap_caps
grep -n 'heap_caps' host/core/src/h_transport_drv.c || echo "  OK: zero heap_caps"
```

- [ ] **Step 3: 搬迁 `transport_util.c` 并修复 DMA 内存分配**

`transport_util.c` 约 200 行，关键问题在第 27 行附近直接调用 `heap_caps_malloc()`：

```bash
cp host/drivers/transport/transport_util.c host/core/src/h_transport_util.c
```

关键修改：

| 行号(约) | 原始引用 | 替换为 |
|:---:|---------|--------|
| #include | `#include "esp_heap_caps.h"` | 删除 |
| ~27 | `heap_caps_malloc(size, MALLOC_CAP_DMA)` | `h_malloc_align(size, 4)` |
| 多行 | `heap_caps_free(ptr)` | `h_free_align(ptr)` |
| #include | `#include "esp_log.h"` | 删除（用 `h_wrapper.h` 中的 H_LOG*） |

```c
// 替换前: heap_caps_malloc(size, MALLOC_CAP_DMA)
// 替换后: h_malloc_align(size, 4)
// 由 ESP-IDF port 的 g_h_osal.malloc_align → heap_caps_aligned_alloc 保证 DMA 能力
```

- [ ] **Step 4: 搬迁后 diff 验证**

```bash
# 确认所有 ESP-IDF 引用已清除
grep -rn 'esp_\|ESP_LOG\|vTaskDelay\|FreeRTOS\|heap_caps' \
    host/core/src/h_transport_drv.c host/core/src/h_transport_util.c
# Expected: zero matches

# 对比搬迁前后的差异（仅确认替换范围）
diff .migration/transport_backup/transport_drv.c host/core/src/h_transport_drv.c \
    | head -200
# 预期：差异仅出现在头文件 include 行、类型名、宏名、API 调用语法
# 不应出现逻辑变更
```

- [ ] **Step 5: Commit**

```bash
git add host/core/src/h_transport_drv.c host/core/src/h_transport_util.c \
        host/core/include/h_internal/h_transport.h
git commit -m "feat(core): migrate transport driver to core layer

Original: host/drivers/transport/transport_drv.c
Original: host/drivers/transport/transport_util.c
Upstream refs: .migration/transport_upstream_ref.txt

Remove ESP-IDF deps: esp_private/wifi.h → h_priv_event.h,
heap_caps_malloc → h_malloc_align, vTaskDelay → h_msleep,
ESP_LOG* → H_LOG*. TLV handshake now uses h_priv_event_t."
```

---

### Task 10: 核心层源文件搬迁——RPC 核心 + Serial IF + Event

> **设计决策——保持文件拆分而非合并**：
>
> 原始设计提议将 `rpc_core.c + rpc_req.c + rpc_rsp.c + rpc_evt.c + rpc_slave_if.c` 合并为单个 `h_rpc_core.c`。评估后决定**保持文件拆分**，理由：
> 1. 5 个文件合计 >2000 行，合并后难以代码审查
> 2. 合并会丢失 git blame 历史，破坏上游 cherry-pick 能力
> 3. 每个文件职责清晰：core=主循环+UID，req=请求，rsp=响应，evt=事件，slave_if=从机接口
>
> **决策**：每个文件独立搬迁、独立去 ESP 化、独立 commit。文件数量增加但风险显著降低。

**Files:**
- Create: `host/core/src/h_rpc_core.c`     (from `host/drivers/rpc/core/rpc_core.c`, ~400 lines)
- Create: `host/core/src/h_rpc_req.c`      (from `host/drivers/rpc/core/rpc_req.c`, ~300 lines)
- Create: `host/core/src/h_rpc_rsp.c`      (from `host/drivers/rpc/core/rpc_rsp.c`, ~250 lines)
- Create: `host/core/src/h_rpc_evt.c`      (from `host/drivers/rpc/core/rpc_evt.c`, ~200 lines)
- Create: `host/core/src/h_rpc_slave_if.c` (from `host/drivers/rpc/slaveif/rpc_slave_if.c`, ~350 lines)
- Create: `host/core/src/h_serial_if.c`    (from `host/drivers/serial/serial_if.c`, ~200 lines)
- Create: `host/core/src/h_event.c`        (new event dispatcher, ~50 lines)
- Create: `host/core/include/h_internal/h_rpc_core.h`
- Create: `host/core/include/h_internal/h_serial_if.h`

**RPC 文件去 ESP-IDF 化通用替换表**（所有 5 个 RPC 文件共用）：

| 原始引用 | 替换为 | 影响文件 |
|---------|--------|---------|
| `#include "esp_task.h"` | 删除（task 配置来自 `h_config.h`） | rpc_core, rpc_req, rpc_rsp, rpc_evt, rpc_slave_if |
| `#include "esp_idf_version.h"` | 使用 `H_IDF_VERSION_MAJOR`/`H_IDF_VERSION_MINOR`（由 `h_port_config.h` 提供） | rpc_req, rpc_rsp |
| `#include "esp_private/wifi.h"` | `#include "h_priv_event.h"` | rpc_slave_if |
| `ESP_LOGI/ESP_LOGE/ESP_LOGW` | `H_LOGI/H_LOGE/H_LOGW` | 全部 5 个文件 |
| `esp_err_t` | `h_err_t` | 全部 5 个文件 |
| `ESP_OK` / `ESP_FAIL` | `H_OK` / `H_FAIL` | 全部 5 个文件 |
| `esp_event_post_to()` | `h_event_post_internal()` | rpc_evt |
| `esp_event_handler_register()` | `h_event_register()` | rpc_core |
| `xQueue*` / `xSemaphore*` (rpc_core.c) | `h_queue_*` / `h_sem_*` wrapper 宏 | rpc_core |

- [ ] **Step 0: 搬迁前快照 + 上游追踪**

```bash
# RPC 核心快照
mkdir -p .migration/rpc_backup
cp -r host/drivers/rpc/core/ .migration/rpc_backup/
cp -r host/drivers/rpc/slaveif/ .migration/rpc_backup/
cp -r host/drivers/serial/ .migration/rpc_backup/

# 记录上游 commit
echo "# RPC core upstream ref" > .migration/rpc_upstream_ref.txt
git log --oneline -5 -- host/drivers/rpc/ >> .migration/rpc_upstream_ref.txt

# 搬迁前记录 ESP-IDF 引用位置
echo "=== ESP-IDF references in RPC files (before) ===" > .migration/rpc_refs_before.txt
for f in host/drivers/rpc/core/*.c host/drivers/rpc/slaveif/*.c host/drivers/serial/*.c; do
    echo "--- $f ---" >> .migration/rpc_refs_before.txt
    grep -n 'esp_task\.h\|esp_idf_version\.h\|esp_private/wifi\.h\|esp_err_t\|ESP_LOG\|ESP_OK\|ESP_FAIL\|esp_event_post_to\|esp_event_handler_register' \
        "$f" >> .migration/rpc_refs_before.txt
done
```

- [ ] **Step 1: 创建 RPC 核心内部头文件**

```c
/* host/core/include/h_internal/h_rpc_core.h */
#ifndef H_RPC_CORE_H
#define H_RPC_CORE_H

#include "h_types.h"
#include <stdint.h>

h_err_t h_rpc_core_init(void);
void    h_rpc_core_deinit(void);

/* Send an RPC request and block until response or timeout */
h_err_t h_rpc_send_request(uint32_t cmd_id, const uint8_t *payload,
                           uint16_t payload_len, uint8_t *response,
                           uint16_t *response_len, uint32_t timeout_ms);

#endif
```

- [ ] **Step 2: 独立搬迁每个 RPC 文件**

```bash
# 逐一搬迁，逐一替换，逐一验证

# h_rpc_core.c — 主循环 + UID 管理 + init (~400 lines)
cp host/drivers/rpc/core/rpc_core.c host/core/src/h_rpc_core.c
# 替换：esp_err_t→h_err_t, ESP_LOG*→H_LOG*, esp_task.h→删除,
#       xQueue*/xSemaphore*→h_queue_*/h_sem_*, ESP_OK/ESP_FAIL→H_OK/H_FAIL
grep -n 'esp_\|ESP_\|vTaskDelay\|FreeRTOS\|xQueue\|xSemaphore' host/core/src/h_rpc_core.c || echo "  OK: clean"

# h_rpc_req.c — 请求序列化 (~300 lines)
cp host/drivers/rpc/core/rpc_req.c host/core/src/h_rpc_req.c
# 替换：esp_err_t→h_err_t, ESP_LOG*→H_LOG*, esp_idf_version.h→删除,
#       esp_task.h→删除
grep -n 'esp_\|ESP_' host/core/src/h_rpc_req.c || echo "  OK: clean"

# h_rpc_rsp.c — 响应反序列化 (~250 lines)
cp host/drivers/rpc/core/rpc_rsp.c host/core/src/h_rpc_rsp.c
# 替换：同上
grep -n 'esp_\|ESP_' host/core/src/h_rpc_rsp.c || echo "  OK: clean"

# h_rpc_evt.c — 事件反序列化 (~200 lines)
cp host/drivers/rpc/core/rpc_evt.c host/core/src/h_rpc_evt.c
# 替换：同上 + esp_event_post_to()→h_event_post_internal()
grep -n 'esp_\|ESP_' host/core/src/h_rpc_evt.c || echo "  OK: clean"

# h_rpc_slave_if.c — 从机接口 (~350 lines)
cp host/drivers/rpc/slaveif/rpc_slave_if.c host/core/src/h_rpc_slave_if.c
# 替换：esp_private/wifi.h→h_priv_event.h, 其余同上
grep -n 'esp_\|ESP_' host/core/src/h_rpc_slave_if.c || echo "  OK: clean"
```

- [ ] **Step 3: 搬迁 serial interface**

```bash
cp host/drivers/serial/serial_if.c host/core/src/h_serial_if.c
```

替换所有 ESP-IDF 依赖。关键：`ESP_LOGI` → `H_LOGI`，删除 `#include "esp_task.h"`。

- [ ] **Step 4: 创建事件分发器**

```c
/* host/core/src/h_event.c */
#include "h_event_post.h"
#include "h_wrapper.h"

h_err_t h_event_post_internal(h_event_base_t base, int32_t event_id,
                              void *event_data, size_t event_data_size)
{
    return h_event_post(base, event_id, event_data, event_data_size);
}
```

- [ ] **Step 5: 验证全部搬迁文件无 ESP-IDF 引用**

```bash
for f in host/core/src/h_rpc_core.c host/core/src/h_rpc_req.c \
         host/core/src/h_rpc_rsp.c host/core/src/h_rpc_evt.c \
         host/core/src/h_rpc_slave_if.c host/core/src/h_serial_if.c; do
    echo "Checking $f..."
    grep -rn 'esp_\|ESP_\|vTaskDelay\|FreeRTOS\|heap_caps' "$f" || echo "  OK: clean"
done
# Expected: all files report "OK: clean"
```

- [ ] **Step 6: Commit（按文件分组提交，保持 git 可追踪）**

```bash
# RPC 核心文件 + 内部头文件
git add host/core/src/h_rpc_core.c host/core/src/h_rpc_req.c \
        host/core/src/h_rpc_rsp.c host/core/src/h_rpc_evt.c \
        host/core/src/h_rpc_slave_if.c \
        host/core/include/h_internal/h_rpc_core.h
git commit -m "feat(core): migrate RPC core files (5 files, kept split)

Original: host/drivers/rpc/core/rpc_core.c → h_rpc_core.c
Original: host/drivers/rpc/core/rpc_req.c  → h_rpc_req.c
Original: host/drivers/rpc/core/rpc_rsp.c  → h_rpc_rsp.c
Original: host/drivers/rpc/core/rpc_evt.c  → h_rpc_evt.c
Original: host/drivers/rpc/slaveif/rpc_slave_if.c → h_rpc_slave_if.c
Upstream refs: .migration/rpc_upstream_ref.txt

Files kept split to preserve git blame history and enable upstream
cherry-pick. All ESP-IDF types/macros/headers replaced with h_* equivalents."

git add host/core/src/h_serial_if.c host/core/src/h_event.c \
        host/core/include/h_internal/h_serial_if.h
git commit -m "feat(core): migrate serial IF + add event dispatcher

Original: host/drivers/serial/serial_if.c → h_serial_if.c
New: h_event.c — thin wrapper routing h_event_post_internal() to port event bus."
```

---

### Task 11: RPC Wrapper + Public API

**Files:**
- Create: `host/core/src/h_rpc_wrap.c` (from `host/drivers/rpc/wrap/rpc_wrap.c`)
- Create: `host/core/src/h_rpc_wrap_api.c` (from `host/api/src/esp_wifi_weak.c`)
- Create: `host/core/src/h_api.c`
- Create: `host/core/include/h_public/h_wifi_api.h`

- [ ] **Step 1: 创建公共 Wi-Fi API 头文件**

```c
/* host/core/include/h_public/h_wifi_api.h */
#ifndef H_WIFI_API_H
#define H_WIFI_API_H

#include "h_types.h"
#include "h_wifi_types.h"

/* Phase 1 minimal Wi-Fi API surface */
h_err_t h_wifi_init(const h_wifi_init_config_t *cfg);
h_err_t h_wifi_deinit(void);
h_err_t h_wifi_set_mode(h_wifi_mode_t mode);
h_err_t h_wifi_get_mode(h_wifi_mode_t *mode);
h_err_t h_wifi_start(void);
h_err_t h_wifi_stop(void);
h_err_t h_wifi_connect(void);
h_err_t h_wifi_disconnect(void);
h_err_t h_wifi_set_config(h_wifi_interface_t ifx, h_wifi_config_t *cfg);
h_err_t h_wifi_get_config(h_wifi_interface_t ifx, h_wifi_config_t *cfg);
h_err_t h_wifi_scan_start(const h_wifi_scan_config_t *cfg);
h_err_t h_wifi_scan_stop(void);
h_err_t h_wifi_scan_get_ap_num(uint16_t *num);
h_err_t h_wifi_scan_get_ap_records(uint16_t *num, h_wifi_ap_record_t *records);

#endif
```

- [ ] **Step 2: 搬迁 RPC wrapper 主体（从 `rpc_wrap.c`）**

```bash
cp host/drivers/rpc/wrap/rpc_wrap.c host/core/src/h_rpc_wrap.c
```

替换：
- `esp_err_t` → `h_err_t`
- `ESP_OK` / `ESP_FAIL` → `H_OK` / `H_FAIL`
- `rpc_wrap.c` 中的 ESP-IDF 相关类型与日志宏 → `h_*` 等价接口
- `wifi_init_config_t` → `h_wifi_init_config_t`
- 所有 ESP-IDF Wi-Fi 类型 → `h_wifi_*` 等价类型

- [ ] **Step 3: 搬迁弱符号兼容层（从 `esp_wifi_weak.c`）**

```bash
cp host/api/src/esp_wifi_weak.c host/core/src/h_rpc_wrap_api.c
```

该文件负责保留 `esp_wifi_*` 弱符号兼容入口，而真正的 RPC 包装主体放在 `h_rpc_wrap.c`。

- [ ] **Step 4: 创建公共 API 实现**

```c
/* host/core/src/h_api.c
 * Public API — thin wrappers that call RPC layer */
#include "h_wifi_api.h"
#include "h_rpc_core.h"

/* All h_wifi_* functions delegate to RPC wrappers in h_rpc_wrap.c.
 * This file provides the public-facing entry points. */
```

- [ ] **Step 5: Commit**

```bash
git add host/core/src/h_rpc_wrap.c host/core/src/h_rpc_wrap_api.c host/core/src/h_api.c \
        host/core/include/h_public/h_wifi_api.h
git commit -m "feat(core): add public Wi-Fi API and RPC wrapper

Split RPC wrapper body and weak API compatibility layer for clearer ownership."
```

---

### Task 12: 全面编译隔离验证

**Files:**
- Create: `scripts/check_core_isolation.sh`

- [ ] **Step 1: 创建隔离检查脚本**

```bash
#!/bin/bash
# scripts/check_core_isolation.sh
# Verify host/core/ contains zero ESP-IDF dependencies

FORBIDDEN=""

# ESP-IDF headers
FORBIDDEN+=$(grep -rn '#include.*esp_' host/core/ --include="*.c" --include="*.h" 2>/dev/null || true)
# ESP-IDF types
FORBIDDEN+=$(grep -rn 'esp_err_t\|esp_event_base_t\|esp_mac_type_t' host/core/ --include="*.c" --include="*.h" 2>/dev/null || true)
# ESP-IDF log macros
FORBIDDEN+=$(grep -rn 'ESP_LOG[IEWDV]' host/core/ --include="*.c" --include="*.h" 2>/dev/null || true)
# ESP-IDF Wi-Fi types (pre-migration)
FORBIDDEN+=$(grep -rn 'wifi_init_config_t\|wifi_config_t\|wifi_ap_record_t' host/core/ --include="*.c" --include="*.h" 2>/dev/null || true)
# FreeRTOS direct calls
FORBIDDEN+=$(grep -rn 'vTaskDelay\|xQueueCreate\|xSemaphoreCreate' host/core/ --include="*.c" --include="*.h" 2>/dev/null || true)
# heap_caps direct calls
FORBIDDEN+=$(grep -rn 'heap_caps_malloc\|heap_caps_free\|heap_caps_get' host/core/ --include="*.c" --include="*.h" 2>/dev/null || true)
# GCC constructor (non-portable)
FORBIDDEN+=$(grep -rn '__attribute__.*constructor' host/core/ --include="*.c" --include="*.h" 2>/dev/null || true)

if [ -n "$FORBIDDEN" ]; then
    echo "ERROR: ESP-IDF or non-portable dependencies found in host/core/:"
    echo "$FORBIDDEN"
    exit 1
fi

echo "OK: host/core/ is ESP-IDF-free and portable"
```

- [ ] **Step 2: 逐文件语法编译**

```bash
for f in host/core/src/*.c; do
    echo "Checking $f..."
    gcc -fsyntax-only \
        -I host/core/include/h_public \
        -I host/core/include/h_internal \
        -I host/port/include \
        -I common/proto \
        -I common/protobuf-c/protobuf-c \
        -I common/transport \
        "$f"
done
```

Expected: all files pass with zero errors (undefined extern symbols are OK at syntax-only level).

- [ ] **Step 3: 运行隔离检查**

```bash
bash scripts/check_core_isolation.sh
```

Expected: `OK: host/core/ is ESP-IDF-free and portable`

- [ ] **Step 4: Commit**

```bash
git add scripts/check_core_isolation.sh
git commit -m "feat(ci): add core layer isolation check script

Verifies host/core/ has zero ESP-IDF headers, types, log macros, FreeRTOS
calls, heap_caps calls, or GCC constructors. Runs in CI Tier 1."
```

---

## 计划 1 完成验证清单

全部任务完成后，运行以下最终验证：

```bash
# 1. 目录结构
tree host/core/ host/port/include/

# 2. 隔离检查
bash scripts/check_core_isolation.sh

# 3. 语法编译
for f in host/core/src/*.c; do
    gcc -fsyntax-only -I host/core/include/h_public -I host/core/include/h_internal \
        -I host/port/include -I common/proto -I common/protobuf-c/protobuf-c \
        "$f" || exit 1
done
echo "ALL CORE FILES COMPILE CLEAN"

# 4. 确认 port/include 接口完整性
ls host/port/include/h_port_contract.h \
   host/port/include/h_wrapper.h \
   host/port/include/h_config.h \
   host/port/include/h_port_config.h
```

**计划 1 产出文件清单**：

```
host/core/include/h_public/
  h_types.h          ← 基础类型 + 错误码
  h_wifi_types.h     ← Wi-Fi 类型（10 个 struct）
  h_event.h          ← 事件 ID + 事件注册 API
  h_wifi_api.h       ← 公共 Wi-Fi 控制 API
host/core/include/h_internal/
  h_init.h           ← 显式初始化入口
  h_rpc_core.h       ← RPC 核心内部接口
  h_transport.h      ← Transport 内部接口
    h_transport_drv.h  ← Transport 驱动内部定义
  h_serial_if.h      ← Serial IF 内部接口
  h_event_post.h     ← 内部事件发布
  h_priv_event.h     ← TLV 握手协议
host/core/src/
  h_init.c           ← init/deinit + contract 校验
  h_rpc_core.c       ← RPC 核心—主循环+UID（from rpc_core.c）
  h_rpc_req.c        ← RPC 请求序列化（from rpc_req.c）
  h_rpc_rsp.c        ← RPC 响应反序列化（from rpc_rsp.c）
  h_rpc_evt.c        ← RPC 事件处理（from rpc_evt.c）
  h_rpc_slave_if.c   ← RPC 从机接口（from rpc_slave_if.c）
    h_rpc_utils.c      ← RPC 公共工具函数
    h_rpc_wrap.c       ← Wi-Fi RPC 包装主体（from rpc_wrap.c）
    h_rpc_wrap_api.c   ← `esp_wifi_*` 弱符号兼容层（from esp_wifi_weak.c）
  h_transport_drv.c  ← 传输驱动
  h_transport_util.c ← 传输工具（DMA 分配）
  h_serial_if.c      ← 串行接口
  h_event.c          ← 事件分发器
  h_api.c            ← 公共 API 实现
host/port/include/
  h_port_contract.h  ← 三合约 vtable 定义
  h_wrapper.h        ← 宏封装层
  h_config.h         ← 配置接口
  h_port_config.h    ← Port 配置模板
    h_test_only.h      ← 单元测试暴露接口
scripts/
  check_core_isolation.sh
.migration/           ← 搬迁快照（用于 diff 验证和上游追踪）
  transport_backup/
  rpc_backup/
  transport_upstream_ref.txt
  rpc_upstream_ref.txt
```

---

## 计划 1 完成后——衔接计划 2

**本计划验证范围**：语法编译（gcc -fsyntax-only）+ 隔离检查（grep）。这两个验证确认核心层不再引用 ESP-IDF，但**不验证功能正确性**。

**功能验证由计划 2（Port 适配）承接**：

| 验证内容 | 计划1 | 计划2 |
|---------|:---:|:---:|
| 核心层 gcc 语法编译通过 | ✅ | — |
| 核心层零 ESP-IDF 引用 | ✅ | — |
| ESP-IDF port 上编译 + 链接通过 | — | ✅ |
| h_wifi_init() / h_wifi_scan_start() 实际调用成功 | — | ✅ |
| TLV 握手协议正确解析 slave 能力 | — | ✅ |
| RPC 请求/响应配对正确 | — | ✅ |
| Linux mock 上 Unity 测试套件通过 | — | ✅ |

**建议的检查点**：在开始计划 2 前，确认计划 1 的以下产出已就位：

```bash
# 确认核心层文件数（至少 13 个 .c 文件）
ls host/core/src/*.c | wc -l        # Expected: >= 13

# 确认无 ESP-IDF 引用
bash scripts/check_core_isolation.sh  # Expected: OK

# 确认语法编译全部通过
for f in host/core/src/*.c; do
    gcc -fsyntax-only -I host/core/include/h_public \
        -I host/core/include/h_internal -I host/port/include \
        -I common/proto -I common/protobuf-c/protobuf-c \
        "$f" || exit 1
done
echo "ALL FILES PASS SYNTAX CHECK"
```
```

# 18.通用transport基线与控制面收口-实施计划

> **本文档性质声明**
>
> 本文档承接 [16.Host通用框架完成度评估与后续迁移大纲.md](./16.Host通用框架完成度评估与后续迁移大纲.md) 中以下两段内容：
>
> - 阶段 2：收口通用 transport 主路径
> - 阶段 3：收口控制面独立性
>
> 本文档也必须遵守 [17.完成度口径、验证体系与兼容层退场-实施计划.md](./17.完成度口径、验证体系与兼容层退场-实施计划.md) 已冻结的治理边界，尤其是：
>
> - transport 快速查阅表
> - 18 / 19 号文档之间必须固定的扩展锚点
> - 兼容层退场判断矩阵
> - 对外表述矩阵
>
> 因此，18 号文档不是独立发明一套 transport 收口思路，而是在 16 号给出的阶段目标和 17 号冻结的治理边界之内，定义“通用 transport 基线”和“控制面独立性”的实施骨架。

> **本文档当前状态**
>
> 当前版本是**骨架版**，先冻结以下内容：
>
> - 文档目标与承接关系
> - 当前代码基线与问题边界
> - 覆盖范围 / 不覆盖范围
> - 通用 transport 基线的目标定义
> - 与 19 号 SPI-HD 协议专题的边界
> - 验收标准骨架
> - 风险与待决问题
> - 后续扩写触发条件
>
> 当前版本**不展开** step 级改造顺序、不承诺文件级改法、不提前写死 contract 字段和实现路径。待第一轮代码验证后，再扩写成完整实施计划。

> **与现有文档的关系**
>
> - [9.Host通用化实施总路线图.md](./9.Host通用化实施总路线图.md) 继续负责总路线图、工作包依赖和里程碑，本文件不替代路线图。
> - [15.Host通用框架跨平台移植方案.md](./15.Host通用框架跨平台移植方案.md) 继续负责任意新平台的通用移植方法，本文件不替代移植方案。
> - [17.完成度口径、验证体系与兼容层退场-实施计划.md](./17.完成度口径、验证体系与兼容层退场-实施计划.md) 负责冻结治理口径与扩展锚点；本文件必须显式继承，而不是重写。
> - 后续 19 号文档只处理 SPI-HD 在通用基线之外的专属协议问题；本文件不提前吞掉 19 号的专题内容。
> - 后续 20 号文档只处理第二平台真实 PoC；本文件只定义“什么叫通用基线收口”，不处理具体 bring-up 方案。

---

**Goal:** 冻结“通用 transport 基线”和“控制面独立性”这两项工作的统一目标、边界、代码锚点与验收骨架，确保后续实现不会与 17 号文档冲突，也不会提前把 19 号文档的 SPI-HD 专题边界写死。

**Architecture:** 本文件把“主运行链路收口”拆成两个强耦合面：

1. 通用 transport 基线收口
2. 控制面独立性收口

前者决定 transport 是否真正进入新框架主运行链，后者决定新框架是否具备最小独立生命线。两者在当前代码中共用同一条主路径，不能被拆成互不相干的实施计划。

**Tech Stack:** 以现有 Host core / port / legacy transport 代码为现实基线；当前版本只冻结实施骨架，不直接承诺代码改造顺序。

**T-shirt size:** 当前骨架版不对完整实施工期作承诺；待第一轮代码验证后，在扩写为 step 级计划时补充正式估算。

---

## 1. 当前代码基线与问题边界

### 1.1 为什么 18 号文档要同时覆盖 transport 与控制面

在 16 号文档中，阶段 2 与阶段 3 被分开描述，是为了突出目标差异：

1. 阶段 2 强调 transport 主路径收口
2. 阶段 3 强调控制面独立性收口

但从当前代码实际看，两者并不是两张分离的皮：

1. transport 生命周期与状态推进仍由 [../../host/core/src/h_transport_drv.c](../../host/core/src/h_transport_drv.c) 这类主链路文件掌握
2. 控制面仍由 [../../host/core/src/h_serial_if.c](../../host/core/src/h_serial_if.c) 与 [../../host/core/src/h_rpc_core.c](../../host/core/src/h_rpc_core.c) 挂在同一条 legacy serial / pserial 路径上
3. 只收 transport、不收控制面，最后仍然会得到“数据面看似通了，但新框架没有最小独立生命线”的半完成状态

因此 18 号文档必须把它们作为“同一条主运行链路”的两个面来写。

### 1.2 当前代码现实的最小锚点

本文件后续所有判断，都以以下文件作为最小现实锚点：

| 锚点 | 当前代码事实 | 对 18 号文档的含义 |
|---|---|---|
| [../../host/port/include/h_port_contract.h](../../host/port/include/h_port_contract.h) | 已定义 transport contract 统一入口 | 18 号文档必须先定义“通用基线最小 contract 能力集合” |
| [../../host/port/include/h_wrapper.h](../../host/port/include/h_wrapper.h) | 已提供 `h_transport_init`、`h_spi_transfer`、`h_sdio_read_block`、`h_uart_read` 等 wrapper 入口 | 18 号文档不能跳过 wrapper 层直接重写一套调用模型 |
| [../../host/core/src/h_init.c](../../host/core/src/h_init.c) | 已用 `h_validate_contracts()` 对选定 transport 做 fail-fast 校验 | 18 号文档需要把“通用基线完成后最少该校验什么”写清楚 |
| [../../host/core/src/h_transport_drv.c](../../host/core/src/h_transport_drv.c) | 当前仍掌握 transport 生命周期与协商主链路，且保留 legacy 兼容痕迹 | 18 号文档需要先定义它未来是“收口到新主路径”还是“作为过渡编排层存在” |
| [../../host/core/src/h_serial_if.c](../../host/core/src/h_serial_if.c) | 当前仍明确桥接 `transport_pserial_*` | 18 号文档必须把控制面最小独立生命线写成正式目标 |
| [../../host/core/src/h_rpc_core.c](../../host/core/src/h_rpc_core.c) | 当前同时存在对 `transport_pserial_*` 的直接调用与对 `h_serial_if_*` 的调用 | 18 号文档必须把控制面“直接 legacy 依赖”和“间接桥接依赖”一起纳入收口目标 |

### 1.3 当前还不能假设的事情

当前版本的 18 号文档，不能先验地假设以下事情已经成立：

1. 任何一种 transport 已经完成新框架主路径迁移
2. 控制面只剩一层 legacy 依赖
3. `h_spi_hd_set_data_lines` 已经被证明应该长期留在 contract 中
4. 只要 transport 基线通了，第二平台 PoC 就一定成立

这些都必须等待后续代码验证，而不能在文档里提前写成既定事实。

---

## 2. 覆盖范围与不覆盖范围

### 2.1 本文档覆盖范围

本文件覆盖以下问题：

1. 什么叫“通用 transport 基线”
2. 什么叫“控制面独立性”
3. 两者当前在代码中各自卡在哪里
4. 18 号文档准备以什么骨架推进后续实施
5. 18 与 19 之间的责任边界如何保持稳定
6. 18 号文档最终应交付什么样的验收结果

### 2.2 本文档不覆盖范围

本文件明确不覆盖：

1. SPI-HD 寄存器协议、DMA 流程、数据线协商的专属专题设计
2. 第二平台硬件选择、板级连线、日志采集和 bring-up 顺序
3. 某个 contract 字段是否重命名、改签名、拆结构体
4. 某个 legacy 文件是否立即删除
5. step 级的文件改动顺序与命令清单

这些内容分别由后续 19 / 20 号文档或 18 号文档的扩写版承接。

---

## 3. 设计决策记录

### 决策 1：当前先写骨架，不直接写满 step 级实施细案

原因不是为了拖延，而是为了避免把当前仍需验证的判断提前写死为实现方案。

当前最容易变化的部分有：

1. 通用 transport 基线应该先收哪一条路径
2. 控制面应该先收 bridge 层还是先收 direct call 层
3. `h_spi_hd_set_data_lines` 这类过渡锚点应保留到哪个阶段

这些如果在骨架阶段就写成“确定步骤”，后面一旦验证结果变化，就会把 18 号文档变成返工中心。

### 决策 2：18 号文档必须显式引用 17 号文档的 A-E 锚点

18 号文档当前不是自由发挥，而是被 17 号文档约束：

1. 锚点 A：通用基线入口
2. 锚点 B：共性数据面入口不得被 SPI-HD 专题提前封死
3. 锚点 C：SPI-HD 数据线协商属于显式过渡扩展锚点
4. 锚点 D：transport 协商生命周期归 18 号所有
5. 锚点 E：验证口径不能分家

如果 18 号文档后续没有显式引用这些锚点，就说明这份骨架没有真正建立在 17 号治理基线之上。

### 决策 3：18 号文档只定义“通用 transport 基线”，不提前吞掉 19 号专题

18 号文档需要覆盖 SPI、SDIO、UART 和 SPI-HD 的共性 transport 基线，但不应把 SPI-HD 的专属协议层问题混进来提前写完。

更准确的分工是：

1. 18 号文档定义“所有 transport 共通部分应该如何进入新主路径”
2. 19 号文档定义“SPI-HD 在共通基线之外还要额外收什么”

### 决策 4：控制面独立性验收必须与 transport 基线验收并列存在

本文件不接受“transport 看起来已收口，所以控制面以后再说”的推进顺序。

理由很简单：

1. 当前控制面仍是 legacy 依赖最直接的地方之一
2. 如果控制面不收口，新框架就没有最小独立生命线
3. 没有最小独立生命线，所谓“通用 transport 主路径已成立”就只是局部成立

---

## 4. 通用 transport 基线的目标定义

### 4.1 本文所说的“通用 transport 基线”是什么

在 18 号文档语境下，“通用 transport 基线”至少包括以下 4 个方面：

1. 生命周期入口：init、deinit、状态推进、失败回滚边界
2. 共性数据面入口：SPI / SDIO / UART 等 transport 的最小读写入口
3. 中断 / GPIO / 协商关系：不要求所有 transport 完全一样，但要求边界清晰
4. 对控制面的支撑能力：能够承载最小控制面闭环，而不是只跑数据面

### 4.2 本文所说的“控制面独立性”是什么

在 18 号文档语境下，控制面独立性不等于“完全没有兼容层”，而是指：

1. 新框架具备一条最小控制面生命线
2. 这条生命线不再把 legacy serial / pserial 路径作为不可替代前提
3. RPC 请求、响应、事件接收的主闭环不再依赖 direct legacy 调用存活

### 4.3 18 号文档最终要证明什么

18 号文档最终需要证明的，不是“所有 transport 一步到位”，而是：

1. 至少一类通用 transport 已能在新主路径上独立成立
2. 控制面已有最小独立生命线
3. transport 主链路与控制面之间的关系已经清晰、稳定、可验证

---

## 5. 与 19 号文档的边界

### 5.1 18 号文档负责什么

18 号文档负责：

1. 定义通用 transport 基线最小能力集合
2. 定义 transport 生命周期与协商的共性主链路
3. 定义控制面最小独立生命线的目标状态
4. 定义共性验收口径

### 5.2 19 号文档负责什么

19 号文档负责：

1. SPI-HD 寄存器协议层
2. SPI-HD DMA 流程与缓冲语义
3. SPI-HD 数据线协商的专题收口
4. SPI-HD 专属验收标准

### 5.3 当前不能越界的地方

在骨架阶段，18 号文档不能越界定义以下内容：

1. SPI-HD 专属寄存器访问 API 如何命名
2. SPI-HD DMA 上下文结构体如何组织
3. SPI-HD 数据线协商最终保留在 OSAL extension 还是迁入 transport contract 的具体签名

这些都应留给 19 号文档在 18 号文档给出的锚点约束内完成。

---

## 6. 当前代码锚点与后续扩写关注面

| 区域 | 当前锚点 | 当前事实 | 后续扩写时需要回答的问题 |
|---|---|---|---|
| contract / wrapper 入口 | [../../host/port/include/h_port_contract.h](../../host/port/include/h_port_contract.h)、[../../host/port/include/h_wrapper.h](../../host/port/include/h_wrapper.h) | 通用入口已经存在，但驱动层尚未真正统一走这套入口 | 通用基线最小 contract 能力集合到底是什么 |
| init / validation | [../../host/core/src/h_init.c](../../host/core/src/h_init.c) | 已有 compile-time transport 选择下的基本 fail-fast 校验 | 基线收口后，校验矩阵要扩到什么程度 |
| transport 编排层 | [../../host/core/src/h_transport_drv.c](../../host/core/src/h_transport_drv.c) | 当前仍掌握 transport 生命周期与协商，且保留 legacy 兼容痕迹 | 它未来是核心编排层、过渡层，还是要继续下沉 |
| 控制面桥接层 | [../../host/core/src/h_serial_if.c](../../host/core/src/h_serial_if.c) | 当前仍桥接 `transport_pserial_*` | 先收 bridge 还是先收 direct call，最小闭环怎么定义 |
| 控制面核心 | [../../host/core/src/h_rpc_core.c](../../host/core/src/h_rpc_core.c) | 当前 direct legacy call 与 `h_serial_if_*` 路径并存 | 如何把 direct call 和间接桥接同时收口 |

---

## 7. 验收标准骨架

当前版本只冻结验收骨架，不定义最终细化测试用例。

### 7.1 通用 transport 基线验收骨架

至少应满足：

1. 至少一类通用 transport 已不再主要依赖 legacy 路径兜底
2. init / deinit / 状态推进 / 最小读写入口已可在新主路径上成立
3. transport 生命周期与失败回滚边界已清晰

### 7.2 控制面独立性验收骨架

至少应满足：

1. 控制面最小闭环不再依赖 direct `transport_pserial_*` 调用才能成立
2. `h_serial_if_*` 是否仍保留 bridge 属性，已有清晰结论
3. 新框架已具备最小独立生命线

### 7.3 与 19 号文档的联合验收边界

18 号文档必须明确：

1. 本文件的验收通过，不等于 SPI-HD 已完成专题收口
2. 19 号文档的验收通过，也不能替代本文件的通用基线验收
3. SPI-HD 最终完成结论，必须同时满足 18 号共性标准与 19 号专题标准

---

## 8. 风险与待决问题

### 风险 1：过早写死 step 级方案导致返工

缓解：当前只冻结骨架，不写文件级改动顺序。

### 风险 2：18 号文档把 19 号专题边界提前吞掉

缓解：凡涉及 SPI-HD 专属协议层的内容，当前只写“边界”不写“方案”。

### 风险 3：只写 transport，不写控制面，导致验收口径失真

缓解：把控制面独立性写成与 transport 基线并列的主目标，而不是附属项。

### 待决问题

当前版本保留以下待决问题，待后续扩写时正式回答：

1. 第一条优先收口的通用 transport 路径具体选择哪一条
2. direct legacy call 与 bridge 层两层控制面依赖的收口顺序
3. `h_spi_hd_set_data_lines` 的长期归属如何判断：18 号文档需先判定它是“过渡锚点”还是“正式扩展位”，19 号文档再基于该判定完成 SPI-HD 专题中的具体使用策略
4. `h_validate_contracts()` 应扩展到什么粒度才足以支撑“基线已收口”的说法

---

## 9. 后续扩写触发条件

18 号文档从骨架版扩写为完整 step 级实施计划，至少应满足以下触发条件：

1. 17 号文档已定稿，A-E 五个锚点不再频繁变化
2. 当前代码现实已完成一轮最小核查，能够确认 18 号文档的重点锚点没有写错方向
3. 团队对“第一条优先收口的通用 transport 路径”已有初步共识
4. 19 号文档骨架版已创建，且其覆盖范围与不覆盖范围经双方确认，不会要求 18 号文档调整已冻结的基线定义

当以上条件满足后，18 号文档再补以下内容：

1. step 级工作包
2. 文件级改动顺序
3. 每一步的验证动作
4. 回退策略与阶段产出

---

## 10. 预留 Work Packages（骨架占位）

当前仅保留工作包名称与扩写映射，不展开具体步骤。

| WP | 名称 | 后续扩写时回答 | 对应 17 号验收标准 |
|---|---|---|---|
| WP 1 | 冻结通用 transport 基线最小能力集合 | contract / wrapper / validation 各自最小需要哪些能力 | 第 1、2、5 条 |
| WP 2 | 冻结 transport 生命周期与协商主链路 | 当前编排层如何收口、哪些协商仍属过渡、哪些已进入正式主路径 | 第 2、3、5 条 |
| WP 3 | 冻结控制面最小独立生命线目标 | `h_serial_if_*` 与 `h_rpc_core.c` 的 legacy 依赖如何收口 | 第 3、5 条 |
| WP 4 | 冻结通用基线验收与验证矩阵 | 哪些验证动作足以支撑“通用基线已成立”的结论 | 第 4、5 条 |
| WP 5 | 把骨架版扩写为 step 级实施计划 | 具体文件、顺序、验证和回退策略如何组织成正式计划 | 第 1-5 条（综合承接） |

### 本表的使用规则

1. 这张表的作用是防止 18 号文档扩写时遗忘 17 号文档已经冻结的验收边界。
2. 表中的“对应 17 号验收标准”是最低承接要求，不代表后续扩写时只能覆盖这些内容。
3. 如果后续 17 号文档的验收标准发生变化，应先更新本表，再扩写 18 号文档的 step 级内容。

---

## 11. 当前版本的使用方式

当前版本用于：

1. 先冻结 18 号文档的目标与边界
2. 为后续 19 号文档保留稳定接口边界
3. 为后续代码验证提供“该验证什么”的骨架

当前版本不用于：

1. 直接指导逐文件改代码
2. 直接作为提交拆分计划
3. 直接作为 19 号 SPI-HD 专题的替代文档

如果后续第一轮代码验证完成，这份文档应在当前结构之上继续扩写，而不是推倒重写。
